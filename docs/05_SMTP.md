# SMTP

- Uses Gmail app password. Set `SMTP_USER` + `SMTP_APP_PASSWORD`.
- Subject: `[HL-ARB] HYPE/USDC edge X.XX bps ≥ 3`
- Body: direction, edge breakdown, role, (simulated) request/response, timestamp.
