# UI (Next.js)

- Two directional edges shown with colors:
  - **red**: edge < 0
  - **yellow**: 0 ≤ edge < threshold
  - **green**: edge ≥ threshold
- Floating overlay shows:
  - **latency (ws echo)** from browser
  - **age (ms)** since last payload
  - **server recv (ms)** included in payload
